import styled from "styled-components";

export const MessageShowStyle = styled.div`
    display: flex;
    justify-content: center;
    position: absolute;
    font-size: 14px;
    bottom: -20px;
    width: 100%;
`;