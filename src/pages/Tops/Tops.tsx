import { FC } from "react";
import ContainerProducts from "../../components/ContainerProducts/ContainerProducts";

const Tops: FC = () => {
  return (
    <ContainerProducts h2="Tops" product="tops" $flex={true} />
  )
}

export default Tops;